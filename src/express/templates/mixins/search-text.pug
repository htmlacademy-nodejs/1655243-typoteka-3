mixin searchText(article, text)
  - const index = article.title.toLowerCase().indexOf(text.toLowerCase());
  a.search__link(href=`/articles/${article.id}`)
    | #{article.title.slice(0, index)}
    b= article.title.slice(index, index + text.length)
    | #{article.title.slice(index + text.length)}
